import { AimDirective } from './aim.directive';

describe('AimDirective', () => {
  it('should create an instance', () => {
    const directive = new AimDirective();
    expect(directive).toBeTruthy();
  });
});
